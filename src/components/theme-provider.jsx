import pkg from '@material-tailwind/react';
const { ThemeProvider: ThemeProvider$1 } = pkg;
export function ThemeProvider({ children }) {
  return <ThemeProvider$1>{children}</ThemeProvider$1>;
}
 
export default ThemeProvider;